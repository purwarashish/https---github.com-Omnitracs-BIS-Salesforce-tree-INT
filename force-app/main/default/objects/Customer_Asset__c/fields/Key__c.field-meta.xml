<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Key__c</fullName>
    <description>Key__c is used by the batch Forecast update class to match Customer Assets that need to be updated with shipment lines or subscription invoices</description>
    <externalId>false</externalId>
    <formula>IF (ISBLANK(Oracle_Id__c), 
    &apos;&apos;, 
    Account__c + Oracle_Id__c 
)</formula>
    <inlineHelpText>Key__c is used by the batch Forecast update class to match Customer Assets that need to be updated with shipment lines or subscription invoices</inlineHelpText>
    <label>Key</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
